# v2.4 更新快速參考

## ?? 三大更新

### 1?? 表單可調整大小 ?

**變更**:
```
FixedSingle → Sizable
新增最小尺寸: 800 × 450
```

**效果**:
- ? 可拖曳邊框
- ? 可最大化
- ? 適應各種螢幕

---

### 2?? CPU 數值更準確 ??

**變更**:
```vb
' 舊版
CInt(cpuUsage)

' 新版
CInt(Math.Round(cpuUsage))
```

**效果**:
- ? 四捨五入計算
- ? 數值更精確
- ? 顏色判斷更準

---

### 3?? CPU 監測獨立運作 ??

**變更**:
```vb
' 舊版：需要連線
If currentState = Connected Then
    cpuTimer.Start()
End If

' 新版：不需連線
cpuTimer.Start()  ' 直接啟動
```

**效果**:
- ? 未連線可監測
- ? 連線時傳送資料
- ? 斷線後繼續運作

---

## ?? 操作指南

### 未連線時
```
1. 點 [Start] → CPU 監測啟動 ?
2. 拖曳邊框 → 調整視窗大小 ?
3. 點最大化 → 全螢幕顯示 ?
4. EEPROM → 禁用（需連線）?
```

### 連線後
```
1. CPU 監測 → 資料自動傳送 ?
2. EEPROM → 功能啟用 ?
3. WS2812 → 自動變色 ?
```

---

## ?? 功能對比

| 功能 | v2.3 | v2.4 |
|------|------|------|
| 調整大小 | ? | ? |
| 最大化 | ? | ? |
| CPU 準確度 | ??? | ????? |
| 未連線監測 | ? | ? |
| 連線監測 | ? | ? |

---

## ? 測試重點

### 1. 視窗操作
- [ ] 拖曳邊框調整
- [ ] 最大化/還原
- [ ] 最小尺寸限制

### 2. CPU 監測
- [ ] 未連線啟動
- [ ] 數值正確顯示
- [ ] 顏色正確變化

### 3. 連線功能
- [ ] 連線後自動傳送
- [ ] EEPROM 正常寫入
- [ ] 斷線後監測繼續

---

## ?? 重要提示

### CPU 顏色判斷臨界值

```
50.4% → 四捨五入 50% → 綠色 ?
50.8% → 四捨五入 51% → 黃色 ?

84.4% → 四捨五入 84% → 黃色 ?
84.8% → 四捨五入 85% → 紅色 ?
```

**v2.3 問題**: 50.8% 顯示 50%（綠色）?  
**v2.4 修正**: 50.8% 顯示 51%（黃色）?

---

## ?? 建議設定

### 螢幕尺寸建議

| 螢幕 | 建議視窗大小 |
|------|------------|
| 1366×768 | 800×450 (最小) |
| 1920×1080 | 960×500 (預設) |
| 2560×1440 | 最大化 |

---

**版本**: v2.4  
**更新**: 2025年1月  
**狀態**: ? 就緒
