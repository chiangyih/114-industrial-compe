# 快速使用指南 (v2.1)

## 程式啟動流程

1. **啟動程式**
 - 自動顯示標題列（含崗位號碼）
   - 自動掃描可用 COM Port
   - 狀態顯示 "Disconnect"（紅色背景，白色粗體字）
   - 表單自動置中顯示

2. **連線裝置**
   - 從下拉選單選擇 COM Port
   - 按 "OPEN" 按鈕
   - 成功後自動顯示 "Connected"（綠色背景，白色粗體字）
   - "Current Time" 開始顯示

3. **使用功能**（必須先連線）

   ### CPU Loading 監測 ⭐ v2.1 改進
   - 按 **"Start"** 按鈕啟動
   - **大型顯示**: CPU 百分比（24pt 粗體）
   - **顏色面板**: 100x100 像素方塊即時變色
     - 0-50%: 🟢 綠色
     - 51-84%: 🟡 黃色
     - ≥85%: 🔴 紅色
   - 同步發送 WS2812 顏色指令到 MCU
   - 按 **"Stop"** 按鈕停止監測
     - 顯示重置為 0%，白色背景
     - Start 按鈕重新啟用

   ### EEPROM 寫入
   - 在左側輸入框輸入 **4 位二進位數字**（只能是 0 或 1）
   - 輸入框說明: "EEPROM (4-bit Binary)"
   - 按 "Write" 按鈕
   - 程式自動：
     1. 驗證格式
     2. 轉換為十進位
     3. 發送到 MCU

4. **關閉連線**
   - 按 "Close" 按鈕
   - CPU 監測自動停止
   - 時間顯示停止
   - 狀態變為 "Disconnect"

5. **結束程式**
   - 按右下角 "Exit" 按鈕
   - 或直接關閉視窗

---

## UI 介面說明 (v2.1)

### 左側：連線管理區（淺青色）
```
┌─────────────────────────────────┐
│ 連線管理 (Device Connect)      │
├─────────────────────────────────┤
│ [COM Port▼]  [OPEN]           │
│              [Close]            │
│   │
│ Device Status: [Disconnect]     │
└─────────────────────────────────┘
```

### 右側：CPU & EEPROM 區（淺粉色）
```
┌──────────────────────────────────────────┐
│ CPU Loading && EEPROM           │
├──────────────────────────────────────────┤
│ CPU Loading (%)   Color Status  [Start] │
│ ┌────────────┐   ┌─────────┐   [Stop]  │
│ │    75%     │   │ 🟡      │ │
│ └────────────┘   └─────────┘            │
│      │
│ EEPROM (4-bit Binary)   │
│ ┌────────┐  [Write]         │
│ │ 1010   │           │
│ └────────┘    │
└──────────────────────────────────────────┘
```

### 頂部：時間顯示
```
Current Time : 2025/1/15 下午 03:45:30
```

### 右下：退出按鈕
```
[Exit]
```

---

## 輸入範例

### ✅ 正確格式
| 輸入 | 十進位 | 說明 |
|------|--------|------|
| 0000 | 0 | 最小值 |
| 0001 | 1 | |
| 1010 | 10 | |
| 1111 | 15 | 最大值 |

### ❌ 錯誤格式（會顯示 "Not BIN Format"）
- `1` - 只有1位
- `11` - 只有2位
- `111` - 只有3位
- `11111` - 超過4位
- `0091` - 包含非0/1字元
- `ABCD` - 非數字
- `10 01` - 包含空格

---

## CPU 監測詳細說明 ⭐ v2.1

### 啟動監測
1. 確保已連線（Connected 狀態）
2. 按 **Start** 按鈕
3. Start 按鈕變為停用（灰色）
4. Stop 按鈕變為啟用

### 顯示說明
- **大型數字**: 顯示 CPU 百分比（例如：75%）
- **顏色方塊**: 
  - 🟢 綠色：CPU ≤ 50%（輕度使用）
  - 🟡 黃色：CPU 51-84%（中度使用）
  - 🔴 紅色：CPU ≥ 85%（高度使用）

### 測試 CPU Loading
可以執行多個程式或使用 `loop.bat` 來提高 CPU 使用率：
```batch
@echo off
:loop
goto loop
```
- 執行 1 個 loop.bat → 約 13-25% CPU（綠色）
- 執行 4-5 個 → 約 60-70% CPU（黃色）
- 執行 8+ 個 → 約 85%+ CPU（紅色）

### 停止監測
1. 按 **Stop** 按鈕
2. 顯示重置：
   - CPU 百分比 → 0%
   - 顏色方塊 → 白色
3. Stop 按鈕變為停用
4. Start 按鈕重新啟用

---

## BLE 協定格式

程式會自動封裝以下格式的封包：

### WS2812 顏色控制（自動發送）
```
AA 01 04 [CPU%] [R] [G] [B] [Checksum] 55
```
範例：CPU 75%, 黃色
```
AA 01 04 4B FF FF 00 4F 55
```

### EEPROM 寫入（按 Write 時發送）
```
AA 02 01 [十進位值] [Checksum] 55
```
範例：輸入 1010 (十進位 10)
```
AA 02 01 0A 0D 55
```

---

## 熱插拔功能

- **插入新裝置**: 等待最多2秒，COM Port 清單自動更新
- **拔除裝置**: 
  - 若已連線，自動斷線並顯示訊息
  - 狀態自動轉為 "Disconnect"

- **手動重新整理**: 點擊 "Device Status" 標籤即可

---

## 疑難排解

### Q: 按 OPEN 後出現「連線失敗」
**A**: 
1. 檢查 COM Port 是否被其他程式占用
2. 確認裝置已正確連接
3. 檢查裝置驅動程式是否安裝

### Q: 出現「寫入逾時」錯誤
**A**:
1. 檢查 USB/藍牙連線是否穩定
2. 確認 MCU 端程式是否正常運作
3. 嘗試重新連線

### Q: CPU Loading 顯示 "CPU Failed"
**A**:
1. 關閉程式重新啟動
2. 檢查系統 Performance Counter 服務是否正常

### Q: 二進位輸入被清空
**A**: 表示格式錯誤，請確保：
- 恰好 4 個字元
- 只包含 0 或 1

### Q: COM Port 清單是空的
**A**:
1. 確認裝置已連接並被系統識別
2. 檢查裝置管理員中是否有 COM Port
3. 點擊 "Device Status" 標籤手動重新整理

### Q: Start 按鈕無法按下 ⭐ v2.1
**A**: 
1. 確認已建立連線（Connected 狀態）
2. 若 CPU 監測正在執行，需先按 Stop

### Q: 顏色方塊不變色 ⭐ v2.1
**A**:
1. 確認 CPU 監測已啟動（按 Start）
2. 等待約 1 秒讓 CPU 數值更新
3. 嘗試執行高 CPU 程式測試

---

## 評分注意事項

1. **標題列**: 會顯示崗位號碼，請確認正確
2. **EEPROM 測試**: 記住你寫入的值（程式會轉成十進位）
3. **CPU 顏色**: 可用 loop.bat 測試顏色變化
   - ⭐ **注意**: 評審會看顏色方塊是否正確變色
4. **連線狀態**: Open 後會自動顯示 Connected（不需手動操作）
5. **Start/Stop**: 確保能正確啟動和停止 CPU 監測
6. **Exit 按鈕**: 應能正常關閉程式

---

## 操作流程檢查表

### 基本操作
- [ ] 程式啟動，標題列正確
- [ ] COM Port 自動列出
- [ ] 選擇 COM Port 並 Open
- [ ] 狀態顯示 Connected（綠色）
- [ ] Current Time 開始顯示

### CPU 監測
- [ ] 按 Start 啟動 CPU 監測
- [ ] CPU 百分比正常顯示
- [ ] 顏色方塊顯示綠色（低 CPU）
- [ ] 執行高 CPU 程式，方塊變黃色/紅色
- [ ] 按 Stop 停止監測
- [ ] 顯示重置為 0%，白色

### EEPROM 寫入
- [ ] 輸入 4 位二進位（例如：1010）
- [ ] 按 Write
- [ ] 顯示寫入成功訊息
- [ ] 嘗試輸入錯誤格式
- [ ] 確認顯示 "Not BIN Format" 並清空

### 結束
- [ ] 按 Close 中斷連線
- [ ] 狀態變為 Disconnect（紅色）
- [ ] 按 Exit 或關閉視窗
- [ ] 程式正常關閉

---

## 快捷鍵

- 無（使用滑鼠操作）

---

## 系統需求

- Windows 10/11
- .NET Framework 4.8
- USB 或藍牙連接埠
- Performance Counter 服務（Windows 內建）

---

## 新功能說明 (v2.1)

### 🎨 視覺化改進
1. **大型 CPU 顯示**: 24pt Arial Bold，清晰易讀
2. **顏色狀態面板**: 100x100 像素，即時變色
3. **清晰標籤**: 每個功能都有說明文字

### 🎮 操作改進
1. **Start/Stop 互鎖**: 同時只能有一個啟用
2. **Exit 按鈕**: 一鍵安全退出
3. **固定視窗**: 防止意外調整大小

### 📱 介面優化
1. **表單置中**: 啟動時自動置中螢幕
2. **下拉選單**: 防止手動輸入錯誤
3. **字體優化**: 更易讀的字型和大小

---

**版本**: v2.1  
**提示**: 所有功能都需要先按 "OPEN" 建立連線！  
**最後更新**: 2025年1月
