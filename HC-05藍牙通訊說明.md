# HC-05 藍牙模組通訊說明文件

**專案**: 114工科賽 Arduino 控制系統  
**藍牙模組**: HC-05  
**通訊對象**: Visual Basic PC 端程式  
**文件日期**: 2025年11月5日

---

## 📋 目錄

1. [目前程式狀況](#目前程式狀況)
2. [HC-05 硬體連接](#hc-05-硬體連接)
3. [通訊協定設計](#通訊協定設計)
4. [Arduino 端程式實作](#arduino-端程式實作)
5. [資料收發流程](#資料收發流程)
6. [除錯與測試](#除錯與測試)

---

## 目前程式狀況

### ⚠️ **重要說明**

**目前的 `main.cpp` 程式中，BLE() 函式並未實作任何藍牙通訊功能！**

目前的 BLE() 函式僅包含：
- ✅ 螢幕顯示 "Connect" 文字
- ✅ 兩個子選單選項（Change Time、Change EEPROM）
- ✅ 按鍵操作（Key0/1/2/3）
- ✅ LED 燈效顯示（呼叫 stripmd()）
- ❌ **沒有** Serial 串列埠初始化
- ❌ **沒有** 藍牙資料接收處理
- ❌ **沒有** 藍牙資料發送處理

### 📍 現有程式位置

```cpp
// 檔案：src/main.cpp
// 行數：245-300

void BLE() {
  tft_w(0, 50, 9, ST77XX_RED, "Connect", 0);
  // ... 僅有選單操作邏輯 ...
  // 缺少藍牙通訊程式碼
}
```

---

## HC-05 硬體連接

### 🔌 接線圖

```
HC-05 藍牙模組              Arduino Uno
┌──────────────┐           ┌──────────────┐
│   VCC        │ ────────► │ 5V (或3.3V)  │
│   GND        │ ────────► │ GND          │
│   TXD        │ ────────► │ RX (Pin 0)   │
│   RXD        │ ◄──┐      │ TX (Pin 1)   │
│   EN/KEY     │    │      │              │
│   STATE      │    │      │ (分壓電路)    │
└──────────────┘    │      └──────────────┘
                    │
               ┌────┴─────┐
               │ 1kΩ      │
               │    ├─────┤
               │ 2kΩ      │
               └──────────┘
                  3.3V 分壓
```

### ⚡ **重要提醒**

1. **HC-05 的 RXD 腳位只能接受 3.3V 電壓**
   - Arduino TX (5V) → 需要分壓電路 → HC-05 RX (3.3V)
   - 使用 1kΩ + 2kΩ 電阻分壓
   
2. **HC-05 的 TXD 輸出 3.3V**
   - Arduino RX 可直接接收（5V 容忍度）
   
3. **使用硬體串列埠（Pin 0, 1）時的限制**
   - 上傳程式時需拔除 HC-05 連接
   - 或使用軟體序列埠（SoftwareSerial）

### 📦 分壓電路計算

```
Arduino TX (5V) ──┬── 1kΩ ──┬── HC-05 RX
                  │         │
                  └── 2kΩ ──┴── GND

輸出電壓 = 5V × (2kΩ / (1kΩ + 2kΩ)) = 3.33V ✓
```

---

## 通訊協定設計

### 📤 **PC → Arduino（接收指令）**

所有指令以 **換行符 `\n`** 結尾。

| 指令格式 | 功能說明 | 參數範圍 | 範例 |
|---------|---------|---------|------|
| `T:HH:MM:SS\n` | 設定時間 | HH:00-23, MM:00-59, SS:00-59 | `T:14:30:00\n` |
| `E:ADDR:DATA\n` | 寫入 EEPROM | ADDR:0-1023, DATA:0-255 | `E:0:255\n` |
| `L:RRGGBB\n` | 設定 LED 顏色 | RRGGBB:000000-FFFFFF (16進位) | `L:FF0000\n` |
| `B:VALUE\n` | 設定 LED 亮度 | VALUE:0-255 | `B:128\n` |
| `S?\n` | 查詢系統狀態 | 無 | `S?\n` |
| `R:ADDR\n` | 讀取 EEPROM | ADDR:0-1023 | `R:0\n` |
| `M:MODE\n` | 切換模式 | MODE:0-3 | `M:1\n` |

### 📥 **Arduino → PC（回應與事件）**

#### **指令回應格式**

| 回應類型 | 格式 | 說明 | 範例 |
|---------|------|------|------|
| 成功 | `OK:功能名稱\n` | 指令執行成功 | `OK:TIME_SET\n` |
| 錯誤 | `ERROR:錯誤類型:訊息\n` | 指令執行失敗 | `ERROR:INVALID_PARAM:0-255\n` |
| 資料回報 | `DATA:類型:值\n` | 查詢結果 | `DATA:EEPROM:0=255\n` |
| 狀態回報 | `STATUS:項目=值\n` | 系統狀態 | `STATUS:MENU=1,LED=FF0000\n` |

#### **主動事件通知**

| 事件類型 | 格式 | 觸發時機 | 範例 |
|---------|------|---------|------|
| 系統就緒 | `READY\n` | Arduino 開機完成 | `READY\n` |
| 按鍵事件 | `KEY:按鍵編號\n` | 使用者按下按鍵 | `KEY:0\n` |
| 選單變更 | `MENU:索引\n` | 選單切換 | `MENU:2\n` |
| 功能進入 | `ENTER:功能名稱\n` | 進入某功能 | `ENTER:BLE_MODE\n` |
| 功能退出 | `EXIT:功能名稱\n` | 離開某功能 | `EXIT:BLE_MODE\n` |
| 計時事件 | `TIMER:秒數\n` | EEPROM 倒數計時 | `TIMER:5\n` |

---

## Arduino 端程式實作

### 1️⃣ **setup() 初始化修改**

在 `setup()` 函式的開頭加入串列埠初始化：

```cpp
void setup() {
  // ==================== 藍牙串列埠初始化 ====================
  Serial.begin(9600);        // HC-05 預設鮑率 9600 bps
  Serial.setTimeout(100);    // 設定接收逾時 100ms
  
  // 等待串列埠就緒（選用，用於除錯）
  while (!Serial && millis() < 3000) {
    ; // 等待最多 3 秒
  }
  
  // ==================== 原有的硬體初始化 ====================
  timer_ini(34286);
  pinMode(RLED_PIN, OUTPUT);
  strip.begin();
  strip.show();
  strip.setBrightness(100);
  
  tft.initR(INITR_BLACKTAB);
  tft.fillScreen(ST77XX_BLACK);
  tft.setRotation(3);
  
  pinMode(TFT_BL, OUTPUT);
  analogWrite(TFT_BL, 168);
  
  // ==================== 開機畫面 ====================
  tft_w(25, 50, 18, ST77XX_RED, "CYIVS", 0);
  tft_w(30, 100, 18, ST77XX_BLUE, "C217", 0);
  delay(2000);
  
  tft.fillScreen(ST77XX_BLACK);
  tft_w(30, 25, 18, ST77XX_RED, "MENU", 0);
  tft_w(0, 70, 12, ST77XX_WHITE, page0_md[0], 0);
  
  // ==================== 藍牙就緒通知 ====================
  delay(500);  // 等待藍牙模組穩定
  Serial.println("READY");              // 通知 PC 端系統就緒
  Serial.println("VERSION:1.0");        // 版本資訊
  Serial.println("DEVICE:CYIVS_C217");  // 裝置識別
}
```

---

### 2️⃣ **新增藍牙指令處理函式**

在 `Time()` 函式之前加入以下函式：

```cpp
// ==================== 藍牙指令解析與處理 ====================
// 功能：接收並執行 PC 端傳來的控制指令
// 參數：cmd - 完整的指令字串（不含換行符）
// 回傳：無（透過 Serial 回傳執行結果）

void processBLECommand(String cmd) {
  cmd.trim();  // 移除前後空白字元
  
  if (cmd.length() == 0) return;  // 忽略空白指令
  
  // ========== 時間設定指令：T:HH:MM:SS ==========
  if (cmd.startsWith("T:")) {
    String timeStr = cmd.substring(2);  // 取得時間字串
    
    // 解析時間（格式：HH:MM:SS）
    int hour = timeStr.substring(0, 2).toInt();
    int minute = timeStr.substring(3, 5).toInt();
    int second = timeStr.substring(6, 8).toInt();
    
    // 參數驗證
    if (hour >= 0 && hour <= 23 && 
        minute >= 0 && minute <= 59 && 
        second >= 0 && second <= 59) {
      
      // TODO: 實際設定 RTC 時間（需要 RTC 模組）
      // rtc.adjust(DateTime(2025, 1, 1, hour, minute, second));
      
      // 在螢幕顯示設定的時間
      tft_w(0, 100, 9, ST77XX_YELLOW, timeStr, 0);
      
      // 回報成功
      Serial.print("OK:TIME_SET:");
      Serial.println(timeStr);
    } else {
      // 參數錯誤
      Serial.println("ERROR:INVALID_TIME:HH:MM:SS");
    }
  }
  
  // ========== EEPROM 寫入指令：E:ADDR:DATA ==========
  else if (cmd.startsWith("E:")) {
    int firstColon = cmd.indexOf(':', 2);
    
    if (firstColon > 0) {
      int addr = cmd.substring(2, firstColon).toInt();
      int data = cmd.substring(firstColon + 1).toInt();
      
      // 參數驗證（Arduino Uno EEPROM 大小為 1024 bytes）
      if (addr >= 0 && addr < 1024 && data >= 0 && data <= 255) {
        EEPROM.write(addr, data);
        
        // 回報成功
        Serial.print("OK:EEPROM_WRITE:ADDR=");
        Serial.print(addr);
        Serial.print(",DATA=");
        Serial.println(data);
      } else {
        Serial.println("ERROR:INVALID_EEPROM:ADDR(0-1023),DATA(0-255)");
      }
    } else {
      Serial.println("ERROR:FORMAT:E:ADDR:DATA");
    }
  }
  
  // ========== LED 顏色設定：L:RRGGBB ==========
  else if (cmd.startsWith("L:")) {
    String colorStr = cmd.substring(2);  // 取得顏色字串（16進位）
    
    if (colorStr.length() == 6) {
      // 16進位字串轉換為數值
      uint32_t color = strtol(colorStr.c_str(), NULL, 16);
      
      // 設定所有 LED 為指定顏色
      strip.fill(color, 0, 8);
      strip.show();
      
      // 在螢幕顯示顏色
      tft_w(0, 110, 9, convert24to16(color), "LED:" + colorStr, 0);
      
      // 回報成功
      Serial.print("OK:LED_SET:");
      Serial.println(colorStr);
    } else {
      Serial.println("ERROR:FORMAT:L:RRGGBB");
    }
  }
  
  // ========== LED 亮度設定：B:VALUE ==========
  else if (cmd.startsWith("B:")) {
    int brightness = cmd.substring(2).toInt();
    
    // 參數驗證（0-255）
    if (brightness >= 0 && brightness <= 255) {
      strip.setBrightness(brightness);
      strip.show();
      
      // 回報成功
      Serial.print("OK:BRIGHTNESS_SET:");
      Serial.println(brightness);
    } else {
      Serial.println("ERROR:INVALID_BRIGHTNESS:0-255");
    }
  }
  
  // ========== 狀態查詢：S? ==========
  else if (cmd == "S?") {
    // 回報系統狀態（可根據需求擴充）
    Serial.print("STATUS:RUNNING");
    Serial.print(",BRIGHTNESS=");
    Serial.print(strip.getBrightness());
    // 可新增更多狀態資訊
    Serial.println();
  }
  
  // ========== EEPROM 讀取：R:ADDR ==========
  else if (cmd.startsWith("R:")) {
    int addr = cmd.substring(2).toInt();
    
    // 參數驗證
    if (addr >= 0 && addr < 1024) {
      int data = EEPROM.read(addr);
      
      // 回報讀取結果
      Serial.print("DATA:EEPROM:ADDR=");
      Serial.print(addr);
      Serial.print(",DATA=");
      Serial.println(data);
    } else {
      Serial.println("ERROR:INVALID_ADDR:0-1023");
    }
  }
  
  // ========== 模式切換：M:MODE ==========
  else if (cmd.startsWith("M:")) {
    int mode = cmd.substring(2).toInt();
    
    // 參數驗證（0-3 對應四個主選單）
    if (mode >= 0 && mode <= 3) {
      // TODO: 觸發對應的功能
      Serial.print("OK:MODE_CHANGE:");
      Serial.println(page0_md[mode]);
    } else {
      Serial.println("ERROR:INVALID_MODE:0-3");
    }
  }
  
  // ========== 未知指令 ==========
  else {
    Serial.print("ERROR:UNKNOWN_CMD:");
    Serial.println(cmd);
  }
}
```

---

### 3️⃣ **修改 BLE() 函式**

完整的藍牙通訊版本：

```cpp
// ==================== BLE 功能（HC-05 藍牙通訊）====================
// 功能：透過 HC-05 模組與 PC 端 Visual Basic 程式進行雙向通訊
// 通訊參數：9600 bps, 8N1, 指令以 '\n' 結尾
// 通訊協定：詳見文件「HC-05藍牙通訊說明.md」

void BLE() {
  // ========== 螢幕顯示初始化 ==========
  tft_w(0, 50, 9, ST77XX_RED, "BT Connect", 0);  // 顯示藍牙連接狀態
  
  // ========== 變數初始化 ==========
  static uint32_t kt = millis();     // 按鍵掃描時間戳記
  int8_t page1 = 0;                  // 子選單索引
  static boolean kf0;                // 按鍵旗標
  String receivedData = "";          // 藍牙接收緩衝區
  uint32_t lastReceiveTime = millis();  // 上次接收資料時間
  
  tft_w(0, 70, 9, ST77XX_GREEN, page_change[page1], 0);
  
  // ========== 通知 PC 進入藍牙模式 ==========
  Serial.println("ENTER:BLE_MODE");
  
  // ========== 主迴圈（藍牙 + 按鍵雙線處理）==========
  while (1) {
    
    // ===== 藍牙資料接收處理 =====
    if (Serial.available() > 0) {
      char inChar = Serial.read();  // 讀取一個字元
      lastReceiveTime = millis();   // 更新接收時間
      
      // 檢查是否為指令結束符號
      if (inChar == '\n') {
        // ========== 收到完整指令，開始處理 ==========
        
        // 在螢幕顯示接收到的指令（除錯用）
        String displayCmd = receivedData;
        if (displayCmd.length() > 18) {
          displayCmd = displayCmd.substring(0, 18);  // 限制顯示長度
        }
        tft_w(0, 90, 9, ST77XX_CYAN, displayCmd + "  ", 0);
        
        // 呼叫指令處理函式
        processBLECommand(receivedData);
        
        // 清空接收緩衝區
        receivedData = "";
      }
      // 忽略 '\r' 字元（Windows 換行為 \r\n）
      else if (inChar != '\r') {
        // 累積字元到緩衝區
        receivedData += inChar;
        
        // ========== 緩衝區溢位保護 ==========
        if (receivedData.length() > 100) {
          Serial.println("ERROR:BUFFER_OVERFLOW");
          receivedData = "";  // 清空緩衝區
          tft_w(0, 90, 9, ST77XX_RED, "OVERFLOW!", 0);
        }
      }
    }
    
    // ===== 接收逾時處理（選用）=====
    if (receivedData.length() > 0 && 
        (millis() - lastReceiveTime) > 1000) {
      // 超過 1 秒未收到完整指令，清空緩衝區
      Serial.println("ERROR:TIMEOUT");
      receivedData = "";
    }
    
    // ===== 按鍵掃描（每 20ms）=====
    if ((millis() - kt) > 20)  
    {
      kt = millis();
      
      // --- 按鍵放開檢測 ---
      if (kf0) {
        if (keyC(0) && keyC(1) && keyC(2) && keyC(3)) {
          kf0 = 0;  // 所有鍵都放開，重置旗標
        }
      } 
      // --- Key0：下一個選項（並通知 PC）---
      else if (!keyC(0)) {
        if (++page1 > 1) page1 = 0;  // 循環切換
        kf0 = 1;
        tft_w(0, 70, 9, ST77XX_GREEN, page_change[page1], 0);
        
        // 回報按鍵事件給 PC
        Serial.print("KEY:0,MENU:");
        Serial.println(page1);
      } 
      // --- Key1：上一個選項（並通知 PC）---
      else if (!keyC(1)) {
        if (--page1 < 0) page1 = 1;  // 循環切換
        kf0 = 1;
        tft_w(0, 70, 9, ST77XX_GREEN, page_change[page1], 0);
        
        // 回報按鍵事件給 PC
        Serial.print("KEY:1,MENU:");
        Serial.println(page1);
      } 
      // --- Key2：確認執行（並通知 PC）---
      else if (!keyC(2)) {
        kf0 = 1;
        
        // 回報執行動作給 PC
        Serial.print("KEY:2,ACTION:");
        Serial.println(page_change[page1]);
        
        // 執行對應的 LED 顯示效果
        stripmd(page1, page_change[page1]);
        
        // 返回 BLE 選單畫面
        tft.fillScreen(ST77XX_BLACK);
        tft_w(15, 25, 12, ST77XX_WHITE, "BLE", 0);
        tft_w(0, 50, 9, ST77XX_RED, "BT Connect", 0);
        tft_w(0, 70, 9, ST77XX_GREEN, page_change[page1], 0);
      } 
      // --- Key3：返回主選單（並通知 PC）---
      else if (!keyC(3)) {
        // 通知 PC 離開藍牙模式
        Serial.println("EXIT:BLE_MODE");
        break;  // 跳出迴圈，返回主選單
      }
    }
  }
  
  // ========== 退出前的延遲 ==========
  delay(100);  // 防止按鍵重複觸發
}
```

---

### 4️⃣ **loop() 主動事件回報（選用）**

在主選單切換時，可以主動通知 PC：

```cpp
void loop() {
  static uint32_t kt = millis();
  static int8_t page0 = 0;
  static int8_t last_page0 = -1;  // 記錄上次選單位置
  static boolean kf0;

  if ((millis() - kt) > 20)  
  {
    kt = millis();
    
    if (kf0) {
      if (keyC(0) && keyC(1) && keyC(2) && keyC(3)) kf0 = 0;
    } 
    else if (!keyC(0)) {
      if (++page0 > 3) page0 = 0;
      kf0 = 1;
      tft_w(0, 70, 12, ST77XX_WHITE, page0_md[page0], 0);
      
      // ========== 通知 PC 選單變更 ==========
      if (page0 != last_page0) {
        Serial.print("MENU:");
        Serial.print(page0);
        Serial.print(",NAME:");
        Serial.println(page0_md[page0]);
        last_page0 = page0;
      }
    } 
    // ... 其他按鍵處理 ...
  }
}
```

---

## 資料收發流程

### 📊 **完整通訊時序圖**

```
時間軸    PC (Visual Basic)              Arduino (HC-05)
  │
  │       開啟 COM3 (9600 bps)
  ├──────────────────────────────────────────►
  │                                          初始化完成
  │       ◄──────────────────────────────── "READY\n"
  │       ◄──────────────────────────────── "VERSION:1.0\n"
  │
  │       發送 "L:FF0000\n" (設定紅色LED)
  ├──────────────────────────────────────────►
  │                                          解析指令
  │                                          執行 strip.fill(0xFF0000)
  │       ◄──────────────────────────────── "OK:LED_SET:FF0000\n"
  │
  │       發送 "B:128\n" (設定亮度50%)
  ├──────────────────────────────────────────►
  │                                          執行 strip.setBrightness(128)
  │       ◄──────────────────────────────── "OK:BRIGHTNESS_SET:128\n"
  │
  │                                          使用者按下 Key0
  │       ◄──────────────────────────────── "KEY:0,MENU:1\n"
  │
  │       發送 "S?\n" (查詢狀態)
  ├──────────────────────────────────────────►
  │                                          讀取狀態
  │       ◄──────────────────────────────── "STATUS:RUNNING,BRIGHTNESS=128\n"
  │
  └───────────────────────────────────────────
```

### 🔄 **接收資料處理流程**

```
Serial.available() > 0 ?
        │
        ├─ NO ──► 繼續掃描按鍵
        │
        └─ YES ─► char c = Serial.read()
                      │
                      ├─ c == '\n' ?
                      │       │
                      │       ├─ YES ─► processBLECommand(receivedData)
                      │       │              │
                      │       │              ├─ 解析指令類型
                      │       │              ├─ 執行對應動作
                      │       │              └─ Serial.println("OK:...")
                      │       │
                      │       └─ NO ──► receivedData += c
                      │                      │
                      │                      └─ 長度檢查（防溢位）
                      │
                      └─ 繼續接收下一個字元
```

### 📤 **發送資料格式範例**

```cpp
// 1. 簡單回應
Serial.println("OK:TIME_SET");

// 2. 帶參數回應
Serial.print("OK:LED_SET:");
Serial.println("FF0000");

// 3. 錯誤訊息
Serial.println("ERROR:INVALID_PARAM:0-255");

// 4. 資料回報（多筆資訊）
Serial.print("STATUS:MENU=");
Serial.print(currentMenu);
Serial.print(",LED=");
Serial.print(currentColor, HEX);
Serial.print(",BRIGHTNESS=");
Serial.println(brightness);
```

---

## 除錯與測試

### 🔍 **Arduino 序列埠監控視窗測試**

開啟 Arduino IDE 的「序列埠監控視窗」（Tools → Serial Monitor）：

1. **設定參數**
   - 鮑率：9600 baud
   - 行結尾：換行 (Newline)

2. **測試指令**

```
輸入指令         預期回應
─────────────────────────────────────────
L:FF0000    →    OK:LED_SET:FF0000
B:128       →    OK:BRIGHTNESS_SET:128
T:14:30:00  →    OK:TIME_SET:14:30:00
E:0:255     →    OK:EEPROM_WRITE:ADDR=0,DATA=255
R:0         →    DATA:EEPROM:ADDR=0,DATA=255
S?          →    STATUS:RUNNING,BRIGHTNESS=128
ABC123      →    ERROR:UNKNOWN_CMD:ABC123
```

### 🐛 **常見問題排除**

| 問題現象 | 可能原因 | 解決方法 |
|---------|---------|---------|
| 無法上傳程式 | HC-05 佔用 RX/TX | 上傳時拔除 HC-05 |
| 收不到資料 | 鮑率不符 | 確認 HC-05 與程式都是 9600 |
| 亂碼 | 電壓不穩 | 加入分壓電路 |
| 指令無回應 | 未加換行符 | 確認指令以 `\n` 結尾 |
| 緩衝區溢位 | 指令過長 | 限制指令長度 < 100 字元 |
| LED 不亮 | 顏色值錯誤 | 確認格式為 6 位 16 進位 |

### 📝 **除錯輔助程式碼**

在 BLE() 函式中加入除錯訊息：

```cpp
void BLE() {
  // ... 初始化 ...
  
  // 除錯：顯示進入狀態
  #ifdef DEBUG
    Serial.println("DEBUG:ENTER_BLE_FUNCTION");
  #endif
  
  while (1) {
    if (Serial.available() > 0) {
      char inChar = Serial.read();
      
      // 除錯：顯示接收的字元（16進位）
      #ifdef DEBUG
        Serial.print("DEBUG:RECV_CHAR:0x");
        Serial.println(inChar, HEX);
      #endif
      
      // ... 處理字元 ...
    }
  }
}
```

啟用除錯模式（在程式最上方）：

```cpp
#define DEBUG  // 啟用除錯訊息
// #undef DEBUG  // 停用除錯訊息
```

---

## 📚 附錄

### A. 完整指令速查表

```
┌─────────────────────────────────────────────────────────┐
│  指令     │  參數                │  功能                 │
├─────────────────────────────────────────────────────────┤
│  T:       │  HH:MM:SS           │  設定時間              │
│  E:       │  ADDR:DATA          │  寫入 EEPROM          │
│  L:       │  RRGGBB             │  設定 LED 顏色         │
│  B:       │  0-255              │  設定 LED 亮度         │
│  S?       │  無                  │  查詢狀態              │
│  R:       │  ADDR               │  讀取 EEPROM          │
│  M:       │  0-3                │  切換模式              │
└─────────────────────────────────────────────────────────┘
```

### B. 錯誤代碼對照表

| 錯誤代碼 | 說明 | 解決方法 |
|---------|------|---------|
| `ERROR:UNKNOWN_CMD` | 未知指令 | 檢查指令格式 |
| `ERROR:INVALID_TIME` | 時間格式錯誤 | 使用 HH:MM:SS 格式 |
| `ERROR:INVALID_EEPROM` | EEPROM 參數錯誤 | ADDR:0-1023, DATA:0-255 |
| `ERROR:FORMAT` | 格式錯誤 | 參考指令格式說明 |
| `ERROR:BUFFER_OVERFLOW` | 緩衝區溢位 | 指令長度 < 100 字元 |
| `ERROR:TIMEOUT` | 接收逾時 | 檢查連線狀態 |

### C. RGB 顏色對照表

| 顏色 | 16進位值 | 指令範例 |
|------|---------|---------|
| 🔴 紅色 | FF0000 | `L:FF0000\n` |
| 🟢 綠色 | 00FF00 | `L:00FF00\n` |
| 🔵 藍色 | 0000FF | `L:0000FF\n` |
| 🟡 黃色 | FFFF00 | `L:FFFF00\n` |
| 🟣 紫色 | FF00FF | `L:FF00FF\n` |
| 🩵 青色 | 00FFFF | `L:00FFFF\n` |
| ⚪ 白色 | FFFFFF | `L:FFFFFF\n` |
| 🟠 橘色 | FF8000 | `L:FF8000\n` |

### D. HC-05 AT 指令設定（選用）

如需修改 HC-05 設定（名稱、鮑率等），需進入 AT 模式：

```
1. 按住 HC-05 的 KEY 按鈕
2. 接上電源
3. LED 慢速閃爍（約 2 秒一次）表示進入 AT 模式
4. 使用 38400 baud 連線

常用 AT 指令：
AT                    → OK (測試連線)
AT+NAME=CYIVS_C217   → OK (設定名稱)
AT+UART=9600,0,0     → OK (設定鮑率)
AT+PSWD=1234         → OK (設定配對密碼)
AT+ROLE=0            → OK (設為從機模式)
```

---

## 📞 技術支援

**文件版本**: 1.0  
**最後更新**: 2025年11月5日  
**適用韌體**: Arduino Uno + HC-05  
**相依函式庫**: Adafruit NeoPixel, Adafruit ST7735

---

**注意**：本文件說明的程式碼需要實際加入到 `main.cpp` 中才能運作，目前的程式尚未包含這些藍牙通訊功能。
